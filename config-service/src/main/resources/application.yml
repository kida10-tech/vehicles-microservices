server:
  port: 8090
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/kida10-tech/vehicles-microservices.git
          search-paths: config-data
  application:
    name: config-service